import { SortPurchasedPipe } from './sort-purchased.pipe';

describe('SortPurchasedPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPurchasedPipe();
    expect(pipe).toBeTruthy();
  });
});
